#include <bits/stdc++.h>
using namespace std;
#define ull long long int
#define pace ios::sync_with_stdio(false);cin.tie(0)
int main()
{
    pace;
    ull q,n;
    cin>>q;
    cout<<q;
   /* for(ull h=0;h<q;h++)
    {
        cin>>n;
        ull s=0;
        for(ull i=1;i<=sqrt(n);i++)
        {
            if(n/i==i)
            {
                s+=i;
            }
            else if(n%i==0)
            {
                s=s+i;
                if(i!=1)
                s=s+(n/i);
            }

        }
        cout<<s<<endl;
    }*/

}